
# Serial Monitor

Лёгкая хуйня сделанная на ебучем Tkinter.
Чё умеет:
- Писать принимаемую и отправляемую хуйню в консоли.
- Отправлять текст и фурри порно на ардуинку.
- Ёбучие кодировки и системы счисления (UTF-8, ANSI, HEX, DEC, BIN).
- Ну графики ёпт.
- Парсеры там какие-то я не ебу как то (Regex or CSV).
- Твои настройки хранятся в ~/.serial_monitor.json.

# Установка

## Вот эту хуйню поставь:

- Python 3.12+
- Tkinter 0.1.0+
- pyserial 3.5+

## Зависимости которые блять listed in pyproject.toml:

```bash
python -m venv venv
# for linux
source venv/bin/activate
# for windows
.venv\Scripts\activate

pip install -r requirements.txt
```

лучше не будь петухом* и юзай poetry:

```bash
poetry install
```

## Как завести жигули деда:

```bash
python -m serial_monitor.app
```

Для не петухов:

```bash
poetry run serial-monitor
```

# Использование данной хуйни
## 1. Главное окно
Тут две хуйни:
1. Консоль
2. График

- Можно уведомить ардуинку о том как сильно ты любишь Лаки с помощью кнопок Enter или Send.
- Отравка фурри порно делается с помощью кнопки 📂.
- DTR и RTS сигналы можно нахуй вырубить если у вас ебучая ESP32-CAM.

### Графики

Ну они блять парсятся в реальном времени.

Можно делать охуенный зум:
1. Колёсико мыши → масштаб по времени (ось X).
2. Shift + Колёсико мыши → масштаб по оси Y. Наверно это значит масштаб по значению, но автор почему-то написал так.

## 2. Соединение

Выбери порт и скорость блять.

Вот нажми на хуйню 🔌.
Если у тя нихуя не подключено, кнопка своим миганием покажет тебе что ты долбоёб.

## 3. Настройки

Открой Settings → Preferences… в меню.

Тут ты можешь настроить:
- Ёбучие кодировки и системы счисления: UTF-8, ANSI, HEX, DEC, BIN
- Чётность (я не ебу чё это): None, Even, Odd, Mark, Space
- DTR/RTS по умолчанию для конченных плат
- Путь для лог файла (сделаем через 20 лет)
- Зарежка отправки между линиями файла
- Конфиг парсера (JSON)

❗Настройки автоматически сохраняются в ~/.serial_monitor.json.

## 4. Парсеры

Парсеры парсят значения для графика.
Например вот блять Regex парсер (в example/temp_n_num.json):

```json
{
  "type": "regex",
  "pattern": "TEMP:(?P<TEMP>[0-9.]+) HUM:(?P<HUM>[0-9.]+)"
}
```

Понять бы чё эта хуйня делает (в example/temp_n_hum_values.txt):


```bash
TEMP:23.4 HUM:50.1
TEMP:22.8 HUM:48.7
```


⬆️Оно выводит TEMP и HUM в график.

# Ярлыки

- Enter → признаться плате в своей любви к Лаки.
- Колёсико мыши → охуенный зум.
- Shift + Колёсико мыши → масштаб по оси Y. Наверно это значит масштаб по значению, но автор почему-то написал так.

Лицензия

MIT — see LICENSE (нейросеть писала)
ПЗД - see GPL (писал дракончик лапками)

*программа написана двумя петухами